#include "Contact.hpp"
#include "PhoneBook.hpp"

int	main(int ac, char **av)
{
	std::cout << "Welcome to my awesome phonebook" << endl;
	while (ac != 2 && (av[1] != "ADD" || av[1] != "SEARCH"))
	while 
	return (0);
}